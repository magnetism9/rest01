# 개발일지

| 항목 | 내용 |
|------|------|
| **날짜** | 2026년 06월 04일 (수) |
| **프로젝트** | 구자성 개인 이력서 웹사이트 |
| **저장소** | https://github.com/magnetism9/rest01 |
| **배포 URL** | https://magnetism9.github.io/rest01/ |
| **작업자** | 구자성 (magnetism9) |

---

## 1. 작업 개요

빈 GitHub 저장소(`rest01`)에 개인 이력서 웹사이트를 처음부터 기획·개발·배포하는 전 과정을 하루 안에 완료하였다.  
총 **12회 커밋**, **18개 파일**, **4,278줄** 의 코드를 작성하였으며 GitHub Pages를 통한 자동 배포 파이프라인까지 구축하였다.  
이후 영역별 기술 등급 평가를 실시하고, 도출된 개선 항목 3건을 즉시 반영하였다.

---

## 2. 단계별 작업 내역

### 2-1. 개발 환경 구성 `f689886`

| 항목 | 내용 |
|------|------|
| 패키지 관리 | `npm` + `live-server` (자동 새로고침 개발 서버) |
| 실행 명령 | `npm run dev` → http://localhost:3000 |
| 폴더 구조 | `css/`, `js/`, `images/`, `scripts/` |
| Git 설정 | `.gitignore` (node_modules 제외) |

**생성 파일**
```
index.html       — 이력서 HTML 골격 (hero / 학력 / 자격 / 역량 / 성격)
css/style.css    — 네이비+오렌지 반응형 디자인 초안
js/main.js       — 스크롤 진입 애니메이션
package.json     — 의존성 관리
```

---

### 2-2. GitHub Pages 자동 배포 `89411e0`

- `.github/workflows/deploy.yml` 작성
- `main` 브랜치 push 시 GitHub Actions가 자동 빌드·배포
- 배포 소요 시간 평균 **20초** 이내
- 별도 빌드 도구 없이 정적 파일을 그대로 배포 (`actions/upload-pages-artifact`)

---

### 2-3. 이력서 콘텐츠 반영 `2180c9c`

GitHub에 업로드된 `dd.pdf`(입사지원서)를 분석하여 실제 이력서 데이터를 HTML에 적용하였다.

**추출된 정보**

| 구분 | 내용 |
|------|------|
| 성명 | 구자성 (Gu Ja Seong) |
| 생년월일 | 2001.05.13 |
| 이메일 | Magentsim99@naver.com |
| 학력 | 경북전문대학교 소방안전관리과 (2020.03 ~ 2024.02, 졸업) |
| 학력 | 한국펫고등학교 경영서비스과 (2017.03 ~ 2020.02, 졸업) |
| 자격 | 운전면허자격 1종보통 (2019.10.07, 경북지방경찰청) |
| 자격 | SMAT 서비스경영자격 3급 (2018.12.05, 한국생산성본부) |
| 자격 | ATC 캐드마스터 2급 (2018.11.14, 한국ATC센터) |

**디자인 개선**
- Hero 섹션: 프로필 사진 영역 + 타이핑 배지 + 연락처
- 학력 타임라인 카드 UI
- 자격증 아이콘 카드 UI
- 보유역량 그리드 / 성격·특기 카드

---

### 2-4. 프로필 사진 적용 `7572eed`

- GitHub에 업로드된 `증명사진.jpg` → `images/profile.jpg` 복사 적용
- `<img onerror>` 이벤트로 사진 없을 시 텍스트 fallback 처리

---

### 2-5. Hero 영역 JavaScript 인터랙션 효과 `2cccf30`

| 효과 | 구현 방식 |
|------|-----------|
| Canvas 파티클 네트워크 | 부유 입자 + 인접 입자 선 연결 (requestAnimationFrame) |
| 타이핑 애니메이션 | 3가지 직함 문구를 타이핑·지우기 루프 |
| 순차 등장 애니메이션 | data-delay 속성 기반 딜레이 페이드인 |
| 프로필 링 펄스 | CSS keyframes — scale + opacity 반복 |

---

### 2-6. GSAP MotionPath SVG 애니메이션 `651fa94`

- GSAP 3.12.5 + MotionPathPlugin CDN 로드
- 복잡한 SVG path 위를 그라데이션 rect가 20초 주기로 순환
- Hero 우측에 반투명 배경 데코로 배치 (z-index 0, pointer-events none)
- 기존 Canvas 파티클과 레이어 분리 공존

---

### 2-7. 컬러 테마 시스템 + 다크모드 `3ba024c`

CSS Custom Property 기반의 5-테마 시스템을 설계·구현하였다.

**테마 파일 구조**
```
css/themes/
├── theme-purple.css  — 다크퍼플 / 옐로우  (기본)
├── theme-blue.css    — 다크블루 / 로열블루
├── theme-green.css   — 다크그린 / 연두
├── theme-rose.css    — 다크로즈 / 코랄핑크
└── theme-teal.css    — 다크틸  / 민트시안
```

**각 테마 파일 변수 구성**
- 라이트 모드 `:root` 변수
- 시스템 다크모드 `@media (prefers-color-scheme: dark)`
- 수동 다크모드 `[data-mode="dark"]`
- 수동 라이트모드 `[data-mode="light"]`

**팔레트 스위처 UI**
- 우측 하단 고정 위젯 (`position: fixed`)
- 🎨 버튼 클릭 시 5개 스와치 패널 오픈
- 🌙 버튼으로 라이트/다크 수동 전환
- `localStorage` 선택 상태 영구 저장

**접근성 적용**
- 모든 버튼 `aria-label`, `aria-expanded` 속성
- ESC 키로 패널 닫기
- `:focus-visible` 포커스 링 (`outline: 3px solid var(--c-accent)`)
- WCAG 대비비: 주요 텍스트-배경 **4.5:1 이상** 확보

---

### 2-8. Open Graph + 파비콘 + OG 이미지 `f8f51e5`

> 이 단계까지 완료 후 **영역별 기술 등급 평가**를 실시하였다.  
> 평가 결과 종합 3.75 / 5점, 즉시 개선 가능 항목 3건을 도출하여 `2-9` 에서 반영하였다.  
> → 상세 내용: [`docs/evaluation-20260604.md`](evaluation-20260604.md)

**OG 메타태그 기재 항목**

| 태그 | 값 |
|------|----|
| `og:url` | https://magnetism9.github.io/rest01/ |
| `og:type` | website |
| `og:site_name` | 구자성 이력서 |
| `og:title` | 구자성 \| 소방안전관리 전문가 |
| `og:description` | 전문 지식 소개 + 자격 정보 |
| `og:image` | images/og-image.png (1200×630) |

**Canvas 기반 이미지 생성** (`scripts/generate-images.js`)
- 사용 라이브러리: `@napi-rs/canvas` (네이티브 바인딩 불필요)
- 한국어 폰트: Windows 시스템 폰트 `malgunbd.ttf` 등록
- OG 이미지 (1200×630): 다크블루 그라디언트 + 도트 그리드 + 글로우 효과
- 파비콘 (64×64): 다크퍼플 원 + 옐로우 강조 호 + '구' 이니셜
- 재생성 명령: `npm run gen:images`

---

### 2-9. 평가보고서 기반 품질 개선 `76ed129`

영역별 기술 등급 평가(평균 3.75 / 5) 결과 도출된 즉시 적용 가능 항목 3건을 반영하였다.

| 개선 항목 | 변경 전 | 변경 후 | 영향 영역 |
|-----------|---------|---------|-----------|
| `@napi-rs/canvas` 의존성 분류 | `dependencies` (런타임) | `devDependencies` (개발용) | 성능 |
| JSON-LD 구조화 데이터 | 없음 | `Person` 스키마 (학력·자격·연락처) | OG/SEO |
| GSAP CDN 무결성 검증 | `integrity` 속성 없음 | SHA-384 해시 + `crossorigin` 추가 | 보안/성능 |

**JSON-LD 등록 데이터 항목**
```json
{
  "@type": "Person",
  "name": "구자성",
  "jobTitle": "소방안전관리 전문가",
  "alumniOf": [ "경북전문대학교 소방안전관리과", "한국펫고등학교 경영서비스과" ],
  "hasCredential": [ "운전면허 1종보통", "SMAT 3급", "ATC 캐드마스터 2급" ],
  "knowsAbout": [ "소방안전관리", "CAD 설계", "서비스경영", "자동차 운전" ]
}
```

**GSAP SRI 해시 (SHA-384)**
```
gsap.min.js          sha384-g4NTh/Iv5PPU4xPyhEWqPcwtNXOvdaDI8LLnyYfyNZOjKJeYQyjzQ9X5275eBjpt
MotionPathPlugin     sha384-lgP6SKZeYZwy7E1FNW3+rGRX/lkJF6ipUUkyET+ZdEhxkLidIF0aewQq2LZaOJ2x
```

**개선 후 등급 변화**

| 영역 | 이전 | 이후 |
|------|------|------|
| 성능 | ★★★☆☆ | ★★★★☆ |
| OG / SEO | ★★★☆☆ | ★★★★☆ |
| 종합 | 3.75 / 5 | **4.0 / 5** |

---

### 2-10. 개발일지·평가보고서 작성 `56a91cf`

- `docs/dev-log-20260604.md` — 본 개발일지 (단계별 작업 내역, 커밋 이력, 기술 스택)
- `docs/evaluation-20260604.md` — 8개 영역 기술 평가, WCAG 대비비 수치, 개선 권고사항

---

## 3. 최종 파일 구조

```
rest01/
├── .github/
│   └── workflows/
│       └── deploy.yml          — GitHub Actions 자동 배포
├── css/
│   ├── style.css               — 레이아웃·컴포넌트 (620줄)
│   └── themes/
│       ├── theme-purple.css    — 테마: 다크퍼플/옐로우
│       ├── theme-blue.css      — 테마: 다크블루/로열블루
│       ├── theme-green.css     — 테마: 다크그린/연두
│       ├── theme-rose.css      — 테마: 다크로즈/코랄핑크
│       └── theme-teal.css      — 테마: 다크틸/민트시안
├── images/
│   ├── profile.jpg             — 프로필 증명사진
│   ├── og-image.png            — SNS 공유 이미지 (1200×630)
│   └── favicon.png             — 파비콘 (64×64)
├── js/
│   └── main.js                 — 인터랙션 JS (222줄)
├── scripts/
│   └── generate-images.js      — OG·파비콘 이미지 생성 스크립트
├── docs/
│   ├── dev-log-20260604.md     — 본 개발일지 (최종 업데이트)
│   └── evaluation-20260604.md  — 평가보고서 (8개 영역 등급)
├── index.html                  — 메인 페이지 (336줄)
├── package.json
└── README.md
```

---

## 4. 커밋 이력 요약

| 번호 | 커밋 해시 | 메시지 | 변경 줄 수 |
|------|-----------|--------|-----------|
| 1 | `792e790` | Initial commit | +2 |
| 2 | `f689886` | feat: 이력서 웹사이트 초기 구조 구성 | +2,876 |
| 3 | `89411e0` | ci: GitHub Pages 자동 배포 워크플로우 추가 | +32 |
| 4 | `2180c9c` | feat: 이력서 PDF 내용 기반으로 웹사이트 완성 | +471 / -244 |
| 5 | `7572eed` | feat: 프로필 사진 추가 | 이진 파일 |
| 6 | `2cccf30` | feat: Hero 영역 JavaScript 인터랙션 효과 추가 | +180 / -17 |
| 7 | `651fa94` | feat: GSAP MotionPath SVG 애니메이션 Hero에 추가 | +53 / -1 |
| 8 | `3ba024c` | feat: 컬러 테마 시스템 + 다크모드 + 팔레트 스위처 구현 | +821 / -200 |
| 9 | `f8f51e5` | feat: Open Graph 메타태그 + 파비콘 + OG 이미지 추가 | +246 / -1 |
| 10 | `56a91cf` | docs: 개발일지 및 평가보고서 작성 | +413 |
| 11 | `76ed129` | fix: 평가보고서 개선 3항목 적용 | +62 / -5 |
| 12 | `현재` | docs: 개발일지 최종 업데이트 | — |

**누적 총계: 18개 파일, +4,278줄 작성**

---

## 5. 사용 기술 스택

| 분류 | 기술 |
|------|------|
| 마크업 | HTML5 (시맨틱 태그) |
| 스타일 | CSS3 (Custom Properties, Grid, Flexbox, @media) |
| 스크립트 | Vanilla JavaScript (ES6+) |
| 애니메이션 | Canvas API, GSAP 3.12 + MotionPathPlugin |
| 이미지 생성 | Node.js + @napi-rs/canvas |
| CI/CD | GitHub Actions (actions/deploy-pages) |
| 배포 | GitHub Pages |
| 개발 서버 | live-server |

---

## 6. 특이사항 및 해결 과정

| 상황 | 해결 방법 |
|------|-----------|
| git 첫 커밋 시 Author unknown 오류 | `git config user.email / user.name` 로컬 설정 |
| `MotionPathHelper` Club 플러그인 사용 불가 | 무료 `MotionPathPlugin`만으로 대체 구현 |
| Yellow 계열 텍스트 대비비 미달 | 악센트 컬러를 텍스트 색으로 사용 지양 → 배지 배경색으로만 사용, 텍스트는 `--c-accent-text` 적용 |
| `@napi-rs/canvas` 한국어 토푸 현상 우려 | Windows 시스템 폰트 `malgunbd.ttf` 명시적 등록으로 해결 |
| `@napi-rs/canvas` 런타임 의존성 오분류 | 평가보고서 검토 후 `devDependencies`로 재분류 |
| GSAP CDN 무결성 검증 부재 | PowerShell로 SHA-384 해시 직접 계산 후 `integrity` 속성 적용 |

---

## 7. 잔여 개선 과제

> 평가보고서에서 도출된 항목 중 미적용 사항 (향후 작업 시 반영 권고)

| 항목 | 난이도 | 기대 효과 |
|------|--------|-----------|
| 스킵 링크 (`Skip to main`) 추가 | 쉬움 | 접근성 ★★★★★ |
| `robots.txt` + `sitemap.xml` 생성 | 쉬움 | SEO 향상 |
| 태블릿 중간 브레이크포인트 (768px) | 중간 | 반응형 완성도 |
| OG 이미지 WebP 포맷 추가 | 중간 | 성능 약 40% 개선 |
| 경력사항 섹션 콘텐츠 보완 | 콘텐츠 | 이력서 완성도 |
| 자기소개 About 섹션 추가 | 콘텐츠 | 이력서 완성도 |

---

*최초 작성: 2026-06-04 / 최종 업데이트: 2026-06-04*
